#! /usr/bin/env bash

# Source common env
# =================================================================

ENV_PATH=${HOME}/.env

# shellcheck source=/dev/null
source "${ENV_PATH}/bashrc.common"

# Own environment
# =================================================================

export MYMAIL=''
PS1="${SF_GREEN}${SF_BOLD}\u@\h ${SF_BLUE}\W ${SF_GREEN}\$ ${SF_NORM}"

log-result "Source file ./.env/bashrc.user"
