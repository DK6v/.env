#!/usr/bin/env bash
# Software version of the user environment.
# Reflects the LAST RELEASED version (matches the latest git tag).
# Bumped as part of each release, together with CHANGELOG.md.
# See CHANGELOG.md for the list of changes.
export ENV_VERSION="0.1.0"
